export const PCO_BASE_URL = "https://api.planningcenteronline.com";
export const CHARACTER_LIMIT = 25000;
export const DEFAULT_LIMIT = 25;
export const MAX_LIMIT = 100;

export enum ResponseFormat {
  MARKDOWN = "markdown",
  JSON = "json",
}
